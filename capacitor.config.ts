import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'in.optimoinfotech',
  appName: 'Optimo IT',
  webDir: 'www/browser',
  server: {
    androidScheme: 'https'
  }
};

export default config;
